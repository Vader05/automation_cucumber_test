<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <!-- ... tu groupId, artifactId, version, properties ... -->
    <groupId>com.ejemplo.api</groupId>
    <artifactId>api-cucumber</artifactId>
    <version>1.0-SNAPSHOT</version>

    <properties>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Agregar variable para la versión del plugin de Xray -->
        <xray.plugin.version>3.5.0</xray.plugin.version> 
    </properties>

    <!-- ... tus dependencias ... -->

    <build>
        <plugins>
            <!-- Plugin para compilar -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                </configuration>
            </plugin>

            <!-- Plugin para ejecutar pruebas (Surefire, sin cambios) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>2.22.2</version>
                <configuration>
                    <includes>
                        <include>**/TestRunner.java</include>
                    </includes>
                </configuration>
            </plugin>
            
            <!-- 🛑 NUEVO: Plugin de Xray para Maven 🛑 -->
            <plugin>
                <groupId>com.xpandit.xray</groupId>
                <artifactId>xray-maven-plugin</artifactId>
                <version>${xray.plugin.version}</version>
                <configuration>
                    <jiraBaseUrl>https://xray.cloud.getxray.app</jiraBaseUrl>
                    <!-- Usamos variables de entorno que ya tienes en GitHub Actions -->
                    <username>${env.XRAY_CLIENT_ID}</username>
                    <password>${env.XRAY_CLIENT_SECRET}</password>
                    
                    <!-- Configuración de importación -->
                    <format>cucumber</format>
                    <reportFile>${project.basedir}/target/cucumber.json</reportFile>
                    <projectKey>P123</projectKey>
                    <!-- CRÍTICO: Indica a Xray que actualice P123-14 con los resultados -->
                    <testExecutionKey>P123-14</testExecutionKey> 
                </configuration>
                <executions>
                    <execution>
                        <phase>post-integration-test</phase>
                        <goals>
                            <goal>import-results</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
