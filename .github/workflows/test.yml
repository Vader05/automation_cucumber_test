name: Run API Tests and Report to Xray

on:
  workflow_dispatch:        # Permite ejecución manual desde GitHub UI
  repository_dispatch:      # Permite ejecución remota desde Jira/Xray vía API

jobs:
  run-tests:
    runs-on: ubuntu-latest

    steps:
      # 📦 1️⃣ Descargar el código del repositorio
      - name: Checkout code
        uses: actions/checkout@v4

      # ☕ 2️⃣ Configurar Java (usa JDK 17)
      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # 🧪 3️⃣ Ejecutar los tests de Cucumber
      - name: Run Cucumber tests
        id: run_tests
        continue-on-error: true
        run: |
          echo "Ejecutando pruebas con Maven..."
          chmod +x mvnw || true
          ./mvnw test -Dcucumber.plugin="json:target/cucumber.json"
          echo "Ejecución de pruebas finalizada."

      # 🔐 4️⃣ Obtener Token de Xray Cloud
      - name: Get Xray Token
        id: get_token
        env:
          XRAY_CLIENT_ID: ${{ secrets.XRAY_CLIENT_ID }}
          XRAY_CLIENT_SECRET: ${{ secrets.XRAY_CLIENT_SECRET }}
        if: always()
        run: |
          echo "Obteniendo token de Xray Cloud..."
          RESPONSE=$(curl -s -X POST "https://xray.cloud.getxray.app/api/v2/authenticate" \
            -H "Content-Type: application/json" \
            -d "{\"client_id\": \"${XRAY_CLIENT_ID}\", \"client_secret\": \"${XRAY_CLIENT_SECRET}\"}")
          
          XRAY_TOKEN=$(echo "$RESPONSE" | tr -d '"')
          if [ -z "$XRAY_TOKEN" ]; then
            echo "❌ ERROR: No se pudo obtener el token de Xray."
            exit 1
          fi

          echo "✅ Token obtenido correctamente."
          echo "XRAY_TOKEN=${XRAY_TOKEN}" >> $GITHUB_ENV

      # 📤 5️⃣ Subir resultados a Xray Cloud
      - name: Upload Cucumber results to Xray (Cloud Cucumber Endpoint)
        if: always()
        env:
          XRAY_TOKEN: ${{ env.XRAY_TOKEN }}
        run: |
          REPORT_FILE="target/cucumber.json"

          echo "Verificando archivo de resultados..."
          if [ ! -f "$REPORT_FILE" ]; then
            echo "❌ ERROR: No se encontró el archivo $REPORT_FILE"
            exit 1
          fi

          FILE_SIZE=$(wc -c < "$REPORT_FILE")
          echo "📏 Tamaño del archivo de reporte: $FILE_SIZE bytes"

          echo "📡 Subiendo resultados de Cucumber a Xray Cloud..."
          RESPONSE=$(curl -s -X POST "https://xray.cloud.getxray.app/api/v2/import/execution/cucumber" \
            -H "Authorization: Bearer ${XRAY_TOKEN}" \
            -H "Content-Type: application/json" \
            -d @"${REPORT_FILE}")

          echo "=================================================="
          echo "📤 RESPUESTA DE XRAY CLOUD:"
          echo "$RESPONSE"
          echo "=================================================="

          if echo "$RESPONSE" | grep -q '"error"'; then
            echo "❌ Error al subir resultados a Xray."
            exit 1
          else
            echo "✅ Resultados subidos exitosamente a Xray Cloud."
          fi

